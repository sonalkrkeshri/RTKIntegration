/**
 * @format
 */

import { AppRegistry } from 'react-native';
import RTKIntegration from './src';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => RTKIntegration);
